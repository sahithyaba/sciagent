# Datasets

The initial dataset is synthetic and generated deterministically with seed 42.

It contains an injected high-PM2.5 event so that the first benchmark has a known ground truth.

Future experiments can add public scientific datasets with their original licenses and citations.
